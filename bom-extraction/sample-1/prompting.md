## 2026-09-17 · project bootstrapped as "lumina", branch bom-extraction
New use case requested by the user: Bill of Materials (BOM) Extraction from Drawings, objective "Drawing Upload → Document Processing → BOM Extraction → Structured BOM Output". Input: `/home/kowsalya.nachimuthu@zucisystems.com/Downloads/3S/260374 COMBINED PID SET 6-1-26.pdf`, a 13-page P&ID set. Explicit instructions: use the existing Lippy Archive (both `usecase-4`'s and `drawing-comparison`'s implementations) as the technical/setup reference for architecture, configuration and workflow, but design the actual skill and extraction logic around what this drawing and the BOM use case actually need — not a copy of a comparison skill. Branch created off `main` (not off `drawing-comparison`) so this is a clean, independent use case, isolated from both prior packs.

## 2026-09-17 · full 13-page survey before designing anything
Per the user's explicit instruction to "review the provided drawing carefully and understand what BOM-related information is present" before building, every one of the 13 pages was rendered to a raster image and inspected before any skill/archive file was written. Findings: pages 1–2 are Legend sheets (line types, P&ID symbols, designation codes, piping-class table, full ISA instrument nomenclature) — essential decoder reference, not BOM content themselves. Pages 5, 7, 9 and 13 are sheets explicitly marked "INTENTIONALLY BLANK". The remaining 7 sheets carry the actual BOM-bearing content: a well-pad gathering manifold (page 3, drawing `-001`), interconnect piping (page 4, `-002`), the V-200 intermediate-pressure bulk separator (page 6, `-005`), the V-600A test separator (page 8, `-007`), IP/test gas metering and pressure control (page 10, `-009`), the V-700 vent stack (page 11, `-010`), and the CA-800 instrument-air compressor package with its filter/dryer train and V-805 receiver (page 12, `-011`) — this last sheet carries the richest data, with a proper manufacturer/model/capacity table across six equipment tags. This survey is what the skill file's unit rule, pack profile, and the bootstrap's coverage-scoping decision are built from — not a template applied blind.

## 2026-09-17 · designed a new skill, bom-extraction, not a reuse of drawing-comparison
Reviewed `drawing-comparison/sample-1`'s skill file and archive as the requested architecture/setup reference. Concluded it does not fit as-is: that skill's whole shape (source/supporting document pair, ADDED/REMOVED/CHANGED/UNCHANGED scale, an ALIGN step) exists to diff two revisions of the same drawing, and this task has one drawing and no revision to diff. Wrote `skills/bom-extraction.md` fresh, in the same Lippy Archive skill-file format (front matter, Steps, The rule, Units, Labels, Finding shape, Pack profile) and reusing what does transfer (grid/position-aware unit identity, a documented pack-profile of drawing-reading failure modes, honest scoping of what a first pass does and does not cover) — see `skills/bom-extraction.md` and `bootstrap.md`'s Brief for the reasoning.

## 2026-09-17 · scoped this pack's coverage: equipment + safety/control valves in full, instrument index counted not detailed
The drawing set carries roughly 27 tags with a full, legible spec (10 equipment items, 10 PSVs, 7 set-pointed control valves) and well over 100 additional instrument bubbles with no independent datasheet — mostly per-loop tags (e.g. the `-504` chemical-injection metering loop, the `600A`/`601A`/`602A` per-compartment separator instrumentation, the `800A`–`800E` instrument-air monitoring loop) that would each need individual review to extract responsibly. Decided, and recorded in `bootstrap.md`'s "What this pack does and does not cover" section, to extract the 27 fully-specified items as this pack's findings, and to record the rest (counted, located by sheet, named where legible) in `actuals/twin/derived/` rather than silently omit them or claim complete tag coverage. See `pivot.md` § 3.
