Converter commands (section 12):
- `lippy templates <skill>` writes empty skeleton per stage
- `lippy import <sheet>` moves label sheet → files
- `lippy export <archive>` moves files → sheet
- `lippy check <archive>` enforces sections 5-8 + digests
- `lippy compare <archive> <run>` writes scorecard (sec 11)
