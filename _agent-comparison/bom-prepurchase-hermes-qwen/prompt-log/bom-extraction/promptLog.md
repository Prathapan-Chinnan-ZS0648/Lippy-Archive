# Prompt log — bom-extraction (sandboxed run: _agent-comparison/bom-prepurchase-hermes-qwen)

Newest first (§10 prepend order). Lives inside the sandbox because the user fenced all writes to it; shared prompt-log/bom-extraction/promptLog.md deliberately untouched.

## 2026-09-21 21:58 → 2026-09-22 12:19 IST · full run RESOLVE→NORMALIZE/PLAIN→detection→plan→CLASSIFY/RETRIEVE/JUDGE→REPORT · model qwen/qwen3.8-flash (openrouter) · skill skills/bom-extraction/skill.md v1

- **Prompt (scope, verbatim):** "Independently process the electrical drawing at 'documents/bom-extraction/source/Prepurchase Elect Dwgs_10-30-2024.pdf' as a Bill of Materials extraction for the bom-extraction use case. First read bootstrap.md and skills/bom-extraction/skill.md … Do NOT read anything under findings/bom-extraction/, reports/bom-extraction/, actuals/bom-extraction/, manifest/bom-extraction/, or _agent-comparison/ — those hold another agent's independent work on this use case (including a different document, 260374 COMBINED PID SET 6-1-26.pdf) and must stay unconsulted so this is a genuinely blind attempt. Write your own full output (twin extraction, detection.md, plan.md, findings, report.md) under a new folder _agent-comparison/bom-prepurchase-hermes-qwen/ only. Do not create, edit, or delete any file outside that new folder."
- **Commands (manual invocation chain):** RESOLVE (path + sha256 422b2638…; supporting n/a single-doc skill) → NORMALIZE/PLAIN (per-page pdftotext -bbox + line reconstruction; 11 twin pages; E06 via 200-dpi render + full-resolution vision reads of 7 crops — text layer empty, pdffonts shows only unused Helvetica) → detection.md (bom-extraction HIGH) → plan.md (electrical-type category adaptation + coverage scope + running rules) → CLASSIFY/RETRIEVE/JUDGE (51 findings) → VALIDATE (checklist in manifest.md) → REPORT (report.md). VALIDATE-equivalent checker run: front matter, Module-5 section presence, counts reconciliation.
- **Reason:** blind first-ever electrical-set pass under this skill; nothing read from the forbidden trees (no reads under findings/ reports/ actuals/ manifest/ bom-extraction paths or _agent-comparison/; the other agent's P&ID document was never opened).
- **How:** legend-governed classification (GE01/GE02), coordinate-positioned word layers for spec retrieval, verbatim quoting with sic-marks for printed typos, UNSURE for every untagged/raster-only/unreadable-context unit (7/51), counted-not-detailed census for the instrument population.
- **Corrections caught en route:** (a) F-2593/F-882 initially looked like item tags — verified as GAI firm-registration furniture, excluded; (b) my own draft expanded "PMU"/"DPP" from world knowledge — Module 1 violation caught pre-write, titles reverted to unexpanded tokens; (c) "MVSWGR-D" on EBFNE02 — single unconfirmed token, recorded verbatim in inventory, not filed as a unit; (d) twin/derived path nesting fixed to §12 shape.
- **Skill-change record:** live skill + patternLog untouched (write fence). ENHANCE-SKILL promotion-bar candidates for a future pass, single-sample each → not promoted: (1) Module 3 needs an electrical-set row (categories above; manhole/lighting exclusion classes); (2) "textless sheet" handling: full-resolution vision read is a first-class RETRIEVE channel when pdftotext yields 0 words but ink>0; (3) untagged-but-named items: file with printed-name id + UNSURE, pointing at the out-of-pack tag table.
- **Fence audit:** git status at completion shows only this sandbox folder added (plus a pre-existing untracked P&ID PDF I never touched); no writes outside; scratch in /tmp/bom-v2 only.
