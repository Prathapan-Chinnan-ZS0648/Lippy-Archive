# Detection


| Quirk | Where observed | Handling |
|---|---|---|
| New document format for this use case: Word document (`.docx`), with no fixed physical pagination in its underlying structure | Both `SYNTH_VERS_DOCS_S3_REVISED.docx` and `SYNTH_VERS_DOCS_S3_ORIGINAL.docx` are Word documents extracted via `python-docx`; unlike a rendered PDF, a `.docx` does not carry a fixed page count independent of the viewer/renderer. | Per `skills/version-compare/skill.md` Module 6 ("Mixed or converted formats"), normalized both documents to extracted per-section text before applying grain and verdicts. The unit of location used is the document's own numbered section (matching its own Table of Contents), not a page — see `sectionMap.md`. This is a third independent document format confirming the format-agnostic normalization rule (after PDF in S1/S2 and `.xlsx` in S4); no skill change needed. |
| Fourth occurrence of self-annotation | The source document marks its own additions/changes with bracketed tags (`[NEW]`, `[MODIFIED]`) throughout most sections, including inside table cells (e.g. Deliverables table rows 5–6, Appendix A row 5). | Already promoted into skill v2 Module 6 (from S1, plus explicit user generalization) and reconfirmed by S2 (PDF) and S4 (`.xlsx`, inside cell text). This is a fourth independent confirmation, now inside a `.docx` document's own prose and table cells — logged here, no further skill change needed. |
| Sections with no body prose, content carried entirely in a table | Sections 6 ("Deliverables"), 7 ("Project Timeline"), 8 ("Evaluation Criteria"), and 13 ("Contact Information") consist of a heading only, with all substantive content in the section's associated table (no paragraph text before or after the table in either document). | No special handling needed — each such section's grain is simply "one table row" for that section (per skill Module 3's tabular-data row), same as any other table-carrying section; the absence of prose paragraphs doesn't change how the table's rows are judged. |
| Row added within an existing table, matched by content | Deliverables table (Section 6): revised has two additional rows ("Catering Services Launch Plan [NEW]", "Winthrop Commons Dining Hall Design & Staffing Plan [NEW]") with no counterpart in the original's five rows. Reference Form table (Appendix B): revised has one additional field row ("Included Catering Program? (Y/N)") with no counterpart in the original's six field rows. Pricing/Commission Form (Appendix A): revised has one additional line item (A-5, "Winthrop Commons dining hall build-out investment") with no counterpart in the original's four rows. | Each judged as an independent row-level ADDED unit (per skill Module 1's granularity-agnostic ADDED rule), mapped by content/label per skill Module 2 stage MAP — not a column-level addition (unlike S4's Requirements Matrix case), since here whole new rows were added to an existing column structure, not a new column added across existing rows. No skill change needed; this is the ordinary row-addition case Module 1/2 already cover. |
| Numeric/date values changed inside otherwise-identical clause wording | E.g. Section 5 (Eligibility Criteria): "5 years" → "7 years", "$20,000,000" → "$30,000,000", "3 reference accounts" → "4 reference accounts"; Section 10 (Commercial Terms): "5 years, with two (2) optional 2-year renewals" → "7 years, with one (1) optional 3-year renewal". | Judged CHANGED, quoting each side's exact figure verbatim per skill Module 1 — no special handling needed; this is the ordinary CHANGED case with a counterpart clause whose value differs. |
| A clause explicitly stating a provision does *not* apply, replaced by a clause stating a provision that *does* apply | Section 10 (Commercial Terms): the original states "No liquidated damages clause applies to this engagement."; the revised issue states a specific liquidated-damages figure and trigger instead, also marked `[NEW]`. | Judged CHANGED, not ADDED — a counterpart clause on the same subject (whether liquidated damages apply) exists in both documents, so this is the same "counterpart exists, only its content/value differs" reasoning already applied to a placeholder value replaced by a real one (`skills/version-compare/patternLog.md` Entry 11(b)), just with an explicit negative statement standing in for the placeholder. The source's own `[NEW]` tag was not treated as authoritative, per skill Module 6. |
| Cover-sheet fields that moved independently of each other | The Cover sheet's RFP Number, Issue Date, and Proposal Submission Deadline all changed between versions, while the Category field ("Campus Auxiliary Services — Dining & Meal Plan Management") and the Estimated Contract Value cross-reference ("See Section 11 — Commercial Terms") are byte-identical in both. | Judged independently per skill Module 1's per-clause grain — each cover-sheet field is its own unit with its own verdict, consistent with the S4 sample's equivalent observation (`skills/version-compare/patternLog.md` Entry 11(c)); no skill change needed, already covered by the existing grain rule. |
